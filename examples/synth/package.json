{
  "name": "mondeto-synth-demo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build-synth": "npx asc -O synth.ts -o synth.wasm",
    "build-key": "npx asc key.ts -o key.wasm",
    "build-slider": "npx asc slider.ts -o slider.wasm",
    "build": "npm run build-synth && npm run build-key && npm run build-slider"
  },
  "author": "Satoshi Tanaka",
  "license": "MIT",
  "devDependencies": {
    "assemblyscript": "^0.17.13"
  },
  "dependencies": {
    "mondeto-as": "file:../../as-lib"
  }
}
